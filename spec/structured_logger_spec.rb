require 'spec_helper'

describe StructuredLogger do
  it 'has a version number' do
    expect(StructuredLogger::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
